server:
  port: 8082

spring:
  application:
    name: customer-service

  # ===============================
  # 🔹 PostgreSQL Configuration (Active)
  # ===============================
  datasource:
    url: jdbc:postgresql://localhost:5433/customer_db
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # ===============================
  # 🔸 MySQL Configuration (Commented - for reference)
  # ===============================
  # datasource:
  #   url: jdbc:mysql://localhost:3306/customer_db
  #   username: root
  #   password: root
  #   driver-class-name: com.mysql.cj.jdbc.Driver
  #
  # jpa:
  #   hibernate:
  #     ddl-auto: update
  #   show-sql: true
  #   properties:
  #     hibernate:
  #       dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/